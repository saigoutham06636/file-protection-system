1) Write a Dart Program to Perform Arithmetic Operations.


void main() {
int sum = 10 + 20;
int diff = 30 - 20;
int mul = 5 * 8;
double div = 90 / 10;

print("Sum Is : $sum");
print("Diff Is : $diff");
print("Prod Is : $mul");
print("Quo Is : $div");
}





2) Write a Dart program to implement different layout structures using Row, Column and Stack
Widgets. 


import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';

void main() {
runApp(
const MaterialApp(
home: Scaffold(body: Center(child: StackWidget())),
),
);
}

class RowWidget extends StatelessWidget {
const RowWidget({super.key});

@override
Widget build(BuildContext context) {
return Row(
children: [
ElevatedButton(
onPressed: () {
print("Button 1 Pressed");
},
child: Text("Button 1"),
),
SizedBox(width: 50),
ElevatedButton(
onPressed: () {
print("Button 2 Pressed");
},
child: Text("Button 1"),
),
SizedBox(width: 50),
ElevatedButton(
onPressed: () {
print("Button 3 Pressed");
},
child: Text("Button 3"),
),
],
);
}
}

class ColWidget extends StatelessWidget {
const ColWidget({super.key});

@override
Widget build(BuildContext context) {
return Column(
children: [
ElevatedButton(
onPressed: () {
print("Button 1 Pressed");
},
child: Text("Button 1"),
),
SizedBox(height: 50),
ElevatedButton(
onPressed: () {
print("Button 2 Pressed");
},
child: Text("Button 2"),
),
SizedBox(height: 50),
ElevatedButton(
onPressed: () {
print("Button 3 Pressed");
},
child: Text("Button 3"),
),
],
);
}
}


class StackWidget extends StatelessWidget {
const StackWidget({super.key});

@override
Widget build(BuildContext context) {
return Stack(
children: [
Container(
height: 200,
width: 200,
color: Colors.red,
),
Container(
height: 150,
width: 150,
color: Colors.blue,
),
Container(
height: 100,
width: 100,
color: Colors.green,
),
],
);
}
}




3) Implement Form Validation and error handling using Dart Language..


import 'package:flutter/material.dart';

void main() {
runApp(
const MaterialApp(
home: Scaffold(body: Center(child: MyWidget())),
),
);
}

class MyWidget extends StatefulWidget {
const MyWidget({super.key});

@override
State<MyWidget> createState() => _MyWidgetState();
}

class _MyWidgetState extends State<MyWidget> {
final usernameController = TextEditingController();
final emailController = TextEditingController();
final passwordController = TextEditingController();

String usernameError = "";
String emailError = "";
String passwordError = "";

void validate() {
String username = usernameController.text.trim();
String email = emailController.text.trim();
String password = passwordController.text.trim();

setState(() {
if (username.isEmpty) usernameError = "Username Empty";

if (!email.contains('@')) emailError = "Enter Valid Email";

if (password.length < 8) passwordError = "Enter a Strong Password";
});
}

@override
Widget build(BuildContext context) {
return Column(
children: [
TextFormField(
controller: usernameController,
decoration: InputDecoration(
label: Text("Username"),
errorText: usernameError.isNotEmpty ? usernameError : null,
),
),
TextFormField(
controller: emailController,
decoration: InputDecoration(
label: Text("Email"),
errorText: emailError.isNotEmpty ? emailError : null,
),
),
TextFormField(
controller: passwordController,
decoration: InputDecoration(
label: Text("Password"),
errorText: passwordError.isEmpty ? null : passwordError,
),
),
ElevatedButton(
onPressed: () {
validate();
},
child: Text("Submit"),
),
],
);
}
}







4) Set up navigation between different screens using navigator in Dart Language.


import 'package:flutter/material.dart';

void main() {
runApp(
const MaterialApp(
home: Scaffold(body: Center(child: NavigatorWidget())),
),
);
}

class NavigatorWidget extends StatelessWidget {
const NavigatorWidget({super.key});

@override
Widget build(BuildContext context) {
return Scaffold(
body: Column(
children: [
Container(
height: 50,
color: Colors.brown,
width: double.infinity,
child: Text(
"This Is First Page",
style: TextStyle(fontSize: 30),
textAlign: TextAlign.center,
),
),
SizedBox(height: 200),
ElevatedButton(
onPressed: () {
Navigator.push(
context,
MaterialPageRoute(builder: (context) => SecondScreen()),
);
},
child: Text("Second Screen"),
),
],
),
);
}
}

class SecondScreen extends StatelessWidget {
const SecondScreen({super.key});

@override
Widget build(BuildContext context) {
return Scaffold(
body: Column(
children: [
Container(
height: 50,
width: double.infinity,
color: Colors.grey,
child: Text(
"This Is Second Page",
style: TextStyle(fontSize: 30),
textAlign: TextAlign.center,
),
),
SizedBox(height: 200),
ElevatedButton(
onPressed: () {
Navigator.pop(context);
},
child: Text("First Screen"),
),
],
),
);
}
}





5)Write a Dart Program to implement different types of animations like fade, slide 


import 'package:flutter/material.dart';
import 'package:flutter/rendering.dart';

void main() {
runApp(
const MaterialApp(
home: Scaffold(body: Center(child: MyWidget())),
),
);
}

class MyWidget extends StatefulWidget {
const MyWidget({super.key});

@override
State<MyWidget> createState() => _MyWidgetState();
}

class _MyWidgetState extends State<MyWidget> {
double x = 0.0;
void handlePressed() {
setState(() {
x = 1.0;
});
}

@override
Widget build(BuildContext context) {
return Column(
children: [
AnimatedOpacity(
duration: Duration(seconds: 5),
opacity: x,
child: Container(height: 100, width: 100, color: Colors.red),
),
Spacer(),
ElevatedButton(
onPressed: () {
handlePressed();
},
child: Text("Fade"),
),
],
);
}
}



import 'package:flutter/material.dart';

void main() {
runApp(
const MaterialApp(
home: Scaffold(body: Center(child: MyWidget())),
),
);
}

class MyWidget extends StatefulWidget {
const MyWidget({super.key});

@override
State<MyWidget> createState() => _MyWidgetState();
}

class _MyWidgetState extends State<MyWidget> {
double yOffset = 0.0;

void handlePressed() {
setState(() {
// change this to a larger value to move further down,
// or set to 0 to move back up (toggle behaviour could be added)
yOffset = 1.5;
});
}

@override
Widget build(BuildContext context) {
return Column(
children: [
// AnimatedSlide moves the widget relative to its normal position
AnimatedSlide(
duration: const Duration(seconds: 2),
offset: Offset(0, yOffset),
child: Container(
height: 100,
width: 100,
color: Colors.red,
child: const Text("Slide"),
),
),
const Spacer(),
ElevatedButton(
onPressed: handlePressed,
child: const Text("Slide"),
),
],
);
}
}





6) Add Animations to UI elements using Flutter's animation framework. 


import 'package:flutter/material.dart';

void main() {
runApp(
const MaterialApp(
home: Scaffold(body: Center(child: MyWidget())),
),
);
}

class MyWidget extends StatefulWidget {
const MyWidget({super.key});

@override
State<MyWidget> createState() => _MyWidgetState();
}

class _MyWidgetState extends State<MyWidget> {
double heightOne = 100;
double widthOne = 100;
void animate() {
setState(() {
heightOne = 200;
widthOne = 200;
});
}

@override
Widget build(BuildContext context) {
return Column(
children: [
AnimatedContainer(
duration: Duration(seconds: 2),
color: Colors.red,
height: heightOne,
width: widthOne,
child: Text("Animation "),
),
Spacer(),
ElevatedButton(
onPressed: () {
animate();
},
child: Text("Animate"),
),
],
);
}
}







7) Implements media queries and breakpoints for responsiveness of different Screens using Dart
Language. 


import 'package:flutter/material.dart';

void main() {
runApp(MyApp());
}

class MyApp extends StatelessWidget {
const MyApp({super.key});

@override
Widget build(BuildContext context) {
return const MaterialApp(
home: HomePage(),
debugShowCheckedModeBanner: false,
);
}
}

class HomePage extends StatefulWidget {
const HomePage({super.key});

@override
State<HomePage> createState() => _HomePageState();
}

class _HomePageState extends State<HomePage> {
@override
Widget build(BuildContext context) {
final currentWidth = MediaQuery.of(context).size.width;

return Scaffold(
backgroundColor: currentWidth < 600
? Colors.deepPurple
: (currentWidth < 1000 ? Colors.yellow : Colors.pink),
body: Center(
child: Text(
currentWidth < 600
? "M O B I L E"
: (currentWidth < 1000 ? "T A B L E T" : "D E S K T O P"),
style: const TextStyle(fontSize: 50, color: Colors.white),
),
),
);
}
}




9) Write a Dart program to explore various flutter widgets like Text, Image, Container etc.. 


import 'package:flutter/material.dart';

void main() {
runApp(const MaterialApp(
home: Scaffold(
body: Center(
// child: ImageWidget(), -> Use This For Image
// child: TextWidget(), -> Use This For Text
// child: ContainerWidget(), -> Use This For Container
),
),
));
}

class ImageWidget extends StatelessWidget {
const ImageWidget({super.key});

@override
Widget build(BuildContext context) {
return Image.network("https://media.istockphoto.com/id/2148071253/photo/chess-game-at-home-checkmate.jpg?s=612x612&w=0&k=20&c=sQNSGy2SVdA2UWizhOIA2z3xngIzfTUexXwsyw4tw9o=");
}
}

class TextWidget extends StatelessWidget {
const TextWidget({super.key});

@override
Widget build(BuildContext context) {
return Text("This Is Text Widget");
}
}

class ContainerWidget extends StatelessWidget {
const ContainerWidget({super.key});

@override
Widget build(BuildContext context) {
return Container(
padding: EdgeInsets.all(5),
decoration: BoxDecoration(
border: Border.all(width: 2)
),
child: Text("I am inside contanier"),
);
}
}






10) Implement navigation with named routes using Dart Language.


import 'package:flutter/material.dart';

void main() {
runApp(
MaterialApp(
initialRoute: '/',

routes: {
'/': (context) => HomePage(),
'/signin': (context) => SigninPage(),
'/login': (context) => LoginPage(),
},
),
);
}

class HomePage extends StatelessWidget {
const HomePage({super.key});

@override
Widget build(BuildContext context) {
return Scaffold(
body: Column(
children: [
Container(
height: 50,
width: double.infinity,
color: Colors.blue,
child: Text(
"This is Inital Page !!",
textAlign: TextAlign.center,
style: TextStyle(fontSize: 30),
),
),
SizedBox(height: 100),
ElevatedButton(
onPressed: () {
Navigator.pushNamed(context, '/signin');
},
child: Text("SignIn"),
),
SizedBox(height: 100),
ElevatedButton(
onPressed: () {
Navigator.pushNamed(context, '/login');
},
child: Text("LogIn"),
),
],
),
);
}
}

class SigninPage extends StatelessWidget {
const SigninPage({super.key});

@override
Widget build(BuildContext context) {
return Scaffold(
body: Column(
children: [
Container(
height: 50,
width: double.infinity,
color: Colors.green,
child: Text(
"This is SignIn Page !!",
textAlign: TextAlign.center,
style: TextStyle(fontSize: 30),
),
),
],
),
);
}
}

class LoginPage extends StatelessWidget {
const LoginPage({super.key});

@override
Widget build(BuildContext context) {
return Scaffold(
body: Column(
children: [
Container(
height: 50,
width: double.infinity,
color: Colors.pink,
child: Text(
"This is LogIn Page !!",
textAlign: TextAlign.center,
style: TextStyle(fontSize: 30),
),
),
],
),
);
}
}





11) Create custom widgets for specific UI elements using Dart Language. 


import 'package:flutter/material.dart';

void main() {
runApp(
const MaterialApp(
home: Scaffold(body: Center(child: MyWidget())),
),
);
}

class MyWidget extends StatefulWidget {
const MyWidget({super.key});

@override
State<MyWidget> createState() => _MyWidgetState();
}

class _MyWidgetState extends State<MyWidget> {
int c = 0;
@override
Widget build(BuildContext context) {
return Column(
children: [
Text("Count Is $c"),
ElevatedButton(
onPressed: () {
setState(() {
c += 1;
});
},
child: Text("Add"),
),
ElevatedButton(
onPressed: () {
setState(() {
c -= 1;
});
},
child: Text("Sub"),
),
],
);
}
}





12) Design a responsive UI that adapts to different screen sizes using Dart Language.


import 'package:flutter/material.dart';

void main() {
runApp(
const MaterialApp(
home: Scaffold(body: Center(child: ResponsivuiWidget())),
),
);
}

class ResponsivuiWidget extends StatelessWidget {
const ResponsivuiWidget({super.key});

@override
Widget build(BuildContext context) {
return Scaffold(
body: Column(
children: [
Container(
width: double.infinity,
height: 50,
color: Colors.red,
child: Text(
"This Is Header",
style: TextStyle(fontSize: 30),
textAlign: TextAlign.center,
),
),
SizedBox(height: 200),
Container(
width: double.infinity,
height: 30,
color: Colors.grey,
child: Text(
"This Is Body Resposive Ui Example",
style: TextStyle(fontSize: 20),
textAlign: TextAlign.center,
),
),
Spacer(),
Container(
width: double.infinity,
height: 15,
color: Colors.blue,
child: Text(
"This Is Footer",
style: TextStyle(fontSize: 10),
textAlign: TextAlign.center,
),
),
],
),
);
}
}







13) Design a form with various input fields using Dart Language. 


import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';

void main() {
runApp(
const MaterialApp(
home: Scaffold(body: Center(child: InputfieldsWidget())),
),
);
}

class InputfieldsWidget extends StatelessWidget {
const InputfieldsWidget({super.key});

@override
Widget build(BuildContext context) {
return Scaffold(
body: Column(
children: [
Container(
width: 500,
child: TextField(
decoration: InputDecoration(
labelText: "Username",
border: OutlineInputBorder(),
),
),
),
SizedBox(height: 50),
Container(
width: 500,
child: TextField(
decoration: InputDecoration(
labelText: "Email",
border: OutlineInputBorder(),
),
),
),
SizedBox(height: 50),
Container(
width: 500,
child: TextField(
obscureText: true,
decoration: InputDecoration(
labelText: "Password",
border: OutlineInputBorder(),
),
),
),
],
),
);
}
}







14)Write a Dart Program for stateful and stateless widgets. 


import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';

void main() {
runApp(
const MaterialApp(
home: Scaffold(body: Center(child: StatefullWidget())),
),
);
}

class StatelesssWidget extends StatelessWidget {
const StatelesssWidget({super.key});

@override
Widget build(BuildContext context) {
return Container(
height: 500,
width: 500,
color: Colors.blue,
child: Text(
"This Is StateLess Widget",
style: TextStyle(fontSize: 20),
textAlign: TextAlign.center,
),
);
}
}

class StatefullWidget extends StatefulWidget {
const StatefullWidget({super.key});

@override
State<StatefullWidget> createState() => _StatefullWidget();
}

class _StatefullWidget extends State<StatefullWidget> {
int count = 0;
@override
Widget build(BuildContext context) {
return Column(
children: [
SizedBox(height: 50),
Text("The Count Is : $count"),
SizedBox(height: 50),
ElevatedButton(
onPressed: () {
setState(() {
count += 1;
});
},
child: Text("Increament"),
),
],
);
}
}







15)Write a Dart program to apply styling using themes and custom styles for UI elements 


import 'package:flutter/material.dart';

void main() {
runApp(
MaterialApp(
theme: ThemeData(
brightness: Brightness.dark,
colorScheme: ColorScheme.fromSeed(seedColor: Colors.blue),
),
home: Scaffold(body: Center(child: MyWidget())),
),
);
}

class MyWidget extends StatelessWidget {
const MyWidget({super.key});

@override
Widget build(BuildContext context) {
final theme = Theme.of(context);
return Scaffold(
body: Container(
height: 50,
width: double.infinity,
child: Text(
"Custom UI",
textAlign: TextAlign.center,
style: TextStyle(fontSize: 30),
),
color: theme.colorScheme.primary,
),
);
}
}